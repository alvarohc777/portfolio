version: '3.7'

services:
  portfolio:
    build:
      context: .
      target: develop
    container_name: portfolio
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - 80:80
    
    entrypoint: /bin/sh -c "quasar dev -p 80"
